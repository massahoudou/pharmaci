<nav class="navbar navbar-expand-lg sticky-top  navbar-light">
  <div class="container">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarTogglerDemo01">
      <a class="navbar-brand" href="{{ path('home')}}">
        <img src="{{asset('logo/fliplogo2.png')}}" alt="" width="" height="80" class="d-inline-block align-text-top">
      </a>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Maladies
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown" style="  max-height: 300px ; overflow-y: auto;">
            {% for maladie in maladies %}
              <li>
                <a href="{{path('maladie',{titre: maladie.titre})}}" class="dropdown-item" style=" padding-inline: 1rem  ">{{maladie.titre}}</a>
              </li>
            {% endfor %}

          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Nutrition
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown2" style="  max-height: 300px ; overflow-y: auto;" >
            {% for nutrition in nutritions %}
              <li>
                <a href="{{path('nutrition',{titre: nutrition.titre})}}" class="dropdown-item" style=" padding-inline: 1rem  ">{{nutrition.titre}}</a>
              </li>
            {% endfor %}
          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown3" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Pays
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown3" style="  max-height: 500px;
                overflow-y: auto;
              ">
            {% for pays in pays %}
              <li>
                <a href="{{path('pays',{pays: pays.nom })}}" class="dropdown-item" style=" padding-inline: 1rem  ">{{pays.nom | capitalize }}</a>
              </li>
            {% endfor %}
          </ul>
        </li>

      </ul>
      <form action="{{ path('recherche')}}" method="get" class="d-flex banner-search ml-2 align-items-center justify-content-center">
        <i class="fas fa-search "></i>
        <input class="form-control " type="search" name="recherche" placeholder="Votre requete" aria-label="Search">
        <button class="btn btn-sante" type="submit">Recherche
        </button>
      </form>
    </div>
  </div>
</nav>
